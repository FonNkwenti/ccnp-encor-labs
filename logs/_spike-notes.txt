SPIKE FINDINGS — 2026-04-20
===========================

1. SESSION LOG DIRECTORY
   Pattern: ~/.claude/projects/<path-with-double-dashes>/
   Example: ~/.claude/projects/C--Users-Nkwenti-Documents-Labs-Cisco-ccnp-encor-labs/
   Rule: Replace os.sep (\ or /) with "--" in the absolute project path, strip leading sep.
   Session files: <session-uuid>.jsonl

2. HOOK STDIN PAYLOAD (to be confirmed once spike hook fires)
   Expected fields based on Claude Code docs and session log inspection:
     - session_id   : UUID matching the .jsonl filename
     - tool_name    : "Skill"
     - tool_input   : { "skill": "<name>", "args": "..." }
     - tool_response: skill output string
   No token data in hook payload — must come from session log.

3. SESSION LOG ENTRY TYPES FOUND
   - "file-history-snapshot": file state snapshots
   - "attachment": rich metadata per turn — includes:
       sessionId, cwd, gitBranch, timestamp, uuid, parentUuid, userType, entrypoint
   - "assistant": LLM turn — includes message.model and message.usage

4. USAGE FIELDS IN ASSISTANT ENTRIES (CONFIRMED)
   message.usage:
     input_tokens                  : int
     output_tokens                 : int
     cache_creation_input_tokens   : int
     cache_read_input_tokens       : int
     server_tool_use               : { web_search_requests, web_fetch_requests }
     service_tier                  : str
     cache_creation                : { ephemeral_1h_input_tokens, ephemeral_5m_input_tokens }
     iterations                    : [ { input_tokens, output_tokens, ... } ]
     speed                         : str

5. MODEL FIELD
   message.model = "claude-sonnet-4-6" (or haiku, opus variant)

6. TIMING
   NOT in hook payload. Must compute with time.monotonic() at script start.

7. ADDITIONAL RICH FIELDS AVAILABLE (bonus — not originally planned)
   "attachment" entries contain cwd + gitBranch which can supplement/replace
   context.py git subprocess calls. Could use these for more reliable context.

8. IMPACT ON IMPLEMENTATION
   - session_log._find_project_dir(): compute from cwd, do NOT iterate all dirs
   - session_log.extract_usage(): field names confirmed — no changes needed
   - context.py: cwd/gitBranch in session log could be used as fallback
   - token data is per-API-call (not cumulative session total)
