---
description: Build labs for a topic sequentially with review gates (Phase 3)
argument-hint: <topic-slug> (e.g. switching, routing, ospf)
---

Invoke the `lab-builder` skill to build labs sequentially for: $ARGUMENTS

If no topic was provided, ask the user which topic (directory under `labs/`) to build and confirm the spec/baseline exist before starting.
